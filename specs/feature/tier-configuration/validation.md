# Validation and Business Rules — Tier Configuration

## Scope

These rules define frontend constraints for tier review and editing.

## Rules

- Tier values shown in the UI must come from configuration data, not static display text.
- The frontend must show the locked tier version once a batch has been calculated.
- The frontend must not imply that tier edits change already calculated batches.
