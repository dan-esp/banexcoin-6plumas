# Validation and Business Rules — Export and Batch History

## Scope

These rules define frontend constraints for export behavior.

## Rules

- Export must remain unavailable before approval.
- Export completion must rely on backend confirmation.
- The UI must distinguish between export readiness, export generation, and export completion.
- If re-export is supported later, the UI must distinguish it from first export.
