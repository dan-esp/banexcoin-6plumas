# Validation and Business Rules — Calculation Results

## Scope

These rules define frontend constraints for displaying calculated outputs.

## Rules

- The frontend must show calculation results as backend-provided values, not recalculate them client-side.
- Warning-attached or high-value accounts should remain visually identifiable.
- The UI must keep payout review separate from export actions until approval is complete.
